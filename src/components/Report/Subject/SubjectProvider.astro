---
import { getReportByCourse } from "@/pages/_metadata/reports";
import Subject from "./Subject.astro";

interface Props {
  studentId: number;
  course: number;
}

const subjectReport = getReportByCourse(Astro.props.course);
---

{subjectReport.map((report) => <Subject {...Astro.props} {...report} />)}
